extends ../../layout

block main
  div(class="container text-zircon px-6")
    div(class="text-3xl font-semibold") Update Corona Virus
    div(class="text-lg")
      | Update berita dari situs
      a(href="https://infeksiemerging.kemkes.go.id/" class="underline")  Infeksi Emerging Kementrian Kesehatan
    for post in posts
      div(class="mt-8 p-4 rounded-md bg-gunmetal")
        a(class="text-sm text-xl font-medium" href=`/posts/${post.id}`)=post.title
        div(class="text-sm mt-4")=moment(post.date).format('DD MMMM YYYY @ HH:mm:ss')
